$(function(){
	$( document ).ready(function() {
		
	});

	$('#m-header_menu').click(function() {
		$('.m-header_body').toggleClass('active');
		/**
		$('.wrap').toggleClass('active-menu');
		$('.m-header_head').toggleClass('active-menu');
  		$('.m-header_body').toggleClass('active-menu');
  		$('.m-header_nav').toggleClass('active-menu');
  		*/
	});

});